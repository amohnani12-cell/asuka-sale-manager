export default async function handler(req,res){const{code}=req.query;if(!code)return res.status(400).json({error:'no code'});const r=await fetch('https://'+process.env.SHOPIFY_STORE+'/admin/oauth/access_token',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({client_id:process.env.SHOPIFY_CLIENT_ID,client_secret:process.env.SHOPIFY_CLIENT_SECRET,code})});const d=await r.json();if(d.access_token){const ev=await fetch('https://api.vercel.com/v10/projects/asuka-sale-manager/env',{headers:{Authorization:'Bearer '+process.env.VERCEL_TOKEN}});const ed=await ev.json();const ex=ed.envs?.find(e=>e.key==='SHOPIFY_TOKEN');if(ex)await fetch('https://api.vercel.com/v10/projects/asuka-sale-manager/env/'+ex.id,{method:'PATCH',headers:{Authorization:'Bearer '+process.env.VERCEL_TOKEN,'Content-Type':'application/json'},body:JSON.stringify({value:d.access_token})});return res.send('<html><body style="font-family:system-ui;padding:40px;background:#111827;color:white;text-align:center"><h1 style="color:#C9A84C;font-size:48px">✓</h1><h2 style="margin-bottom:12px">Connected!</h2><p style="color:#9CA3AF">Redirecting to Sale Manager...</p><script>setTimeout(()=>location.href="/",4000)<\/script></body></html>');}return res.status(400).json({error:'token exchange failed',data:d});}